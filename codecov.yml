# codecov.yml — Codecov configuration for unified coverage reporting
# Issue #424 — Part of unified coverage initiative (Issue #422)
#
# Flags:
#   pester — PowerShell tests covering scripts/
#   pytest — Python tests covering src/

codecov:
  require_ci_to_pass: false

coverage:
  precision: 2
  round: down
  range: "80...100"
  status:
    project:
      default:
        target: auto
        threshold: 1%
      pester:
        flags:
          - pester
        target: auto
        threshold: 1%
      pytest:
        flags:
          - pytest
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 5%

flags:
  pester:
    paths:
      - scripts/
    carryforward: true
  pytest:
    paths:
      - src/
    carryforward: true

parsers:
  jacoco:
    partials_as_hits: true

comment:
  layout: "header, diff, flags, files"
  behavior: default
  require_changes: false
  show_carryforward_flags: true

ignore:
  - "docs/**"
  - "deploy/**"
  - "workflows/**"
  - "config/**"
  - "external/**"
  - "logs/**"
  - "tests/**"
