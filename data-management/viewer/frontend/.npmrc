# eslint-plugin-react@7.37.5 and eslint-plugin-jsx-a11y@6.10.2 cap their
# peer-dep range at ESLint 9. Both plugins function correctly under ESLint 10.
# Remove this file once both plugins publish ESLint 10-compatible peer ranges
# (tracked as WI-07 in the frontend major upgrades planning log).
legacy-peer-deps=true
