# Dev cluster admission overlay.
# Mode: Sigstore keyless (fast iteration, no AKV cost). Production uses
# Notation + AKV HSM (see ../production/admission/kustomization.yaml).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/admission/kyverno-sigstore-policy.yaml
  - ../../../base/admission/trusted-root-kustomization.yaml
