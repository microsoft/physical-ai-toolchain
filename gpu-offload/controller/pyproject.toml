[project]
name = "gpu-offload-controller"
version = "0.0.0"
description = "Hardened Xavier-style admission mutate controller (gpu-offload)"
requires-python = ">=3.12"

dependencies = [
    "kubernetes==35.0.0",
    "PyYAML==6.0.2",
]

[project.optional-dependencies]
test = [
    "pytest==9.1.1",
]

[tool.uv]
package = false
