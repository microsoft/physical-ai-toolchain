{
  "buckets": {
    "${DATASET_BUCKET_NAME}": {
      "dataset_path": "azure://${STORAGE_ACCOUNT_NAME}/${DATASET_CONTAINER_NAME}",
      "region": "${AZURE_REGION}",
      "description": "Dataset storage using Azure Workload Identity",
      "mode": "read-write"
    }
  },
  "default_bucket": "${DATASET_BUCKET_NAME}"
}
